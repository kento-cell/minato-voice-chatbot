# Plain PyPI torch (CPU) for maximum portability -- works on any machine, no GPU required.
# If you have an NVIDIA GPU and want faster inference, install the CUDA build instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch==2.13.0
transformers==5.12.1
peft==0.19.1
trl==1.6.0
datasets==5.0.0
accelerate==1.14.0
requests==2.34.2
sounddevice==0.5.5
soundfile==0.14.0
flask==3.1.3
faster-whisper==1.2.1
